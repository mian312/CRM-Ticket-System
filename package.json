{
  "name": "crm-ticket-system",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup": "npm install && cd Backend && npm i && cd ../Frontend && npm i",
    "serve": "concurrently --kill-others-on-fail \"cd frontend && npm run dev\" \"cd backend && npm run start\""
  },
  "keywords": [],
  "author": "Milan Dey",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
